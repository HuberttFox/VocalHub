{
  "name": "vocalhub",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "npm run build:web && npm run build:worker && npm run build:maintenance",
    "build:web": "next build",
    "build:worker": "esbuild worker/sync-vocadb.ts --bundle --platform=node --format=esm --target=node20 --packages=external --outfile=build/worker/sync-vocadb.mjs",
    "build:maintenance": "esbuild maintenance/cleanup-auth-sessions.ts --bundle --platform=node --format=esm --target=node20 --packages=external --outfile=build/maintenance/cleanup-auth-sessions.mjs",
    "start": "next start",
    "start:worker": "node build/worker/sync-vocadb.mjs",
    "start:session-cleanup": "node build/maintenance/cleanup-auth-sessions.mjs",
    "lint": "node node_modules/eslint/bin/eslint.js .",
    "test": "vitest run tests/unit",
    "test:unit": "vitest run tests/unit",
    "test:integration": "vitest run tests/integration",
    "benchmark:catalog": "tsx benchmarks/catalog/cli.ts",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "sync:vocadb": "tsx worker/sync-vocadb.ts",
    "cleanup:auth-sessions": "tsx maintenance/cleanup-auth-sessions.ts"
  },
  "engines": {
    "node": ">=20.19"
  },
  "dependencies": {
    "@auth/prisma-adapter": "2.11.3",
    "@prisma/adapter-pg": "7.9.1",
    "@prisma/client": "7.9.1",
    "dotenv": "^17.4.2",
    "next": "16.2.12",
    "next-auth": "5.0.0-beta.32",
    "pg": "^8.22.0",
    "react": "latest",
    "react-dom": "latest",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "latest",
    "@types/node": "latest",
    "@types/pg": "^8.20.0",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "esbuild": "^0.28.1",
    "eslint": "latest",
    "eslint-config-next": "latest",
    "prisma": "7.9.1",
    "tailwindcss": "latest",
    "tsx": "^4.23.1",
    "typescript": "latest",
    "vitest": "^4.1.10"
  }
}
